def twosum(num,target):
    left = 0
    right = len(num) - 1
    while left < right:
        current_sum = num[left] + num[right]
        if current_sum == target:
            return (left, right)
        elif current_sum < target:
            left += 1
        else:
            right -= 1
    return None



